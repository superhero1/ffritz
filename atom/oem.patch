--- orig/etc/init.d/rc.conf	2019-09-18 11:29:16.000000000 -0400
+++ squashfs-root/etc/init.d/rc.conf	2019-10-13 02:50:39.711205740 -0400
@@ -391,15 +391,17 @@
 ##########################################################################################
 ## OEM Ermitteln
 ##########################################################################################
-OEM_tmp=`cat $CONFIG_ENVIRONMENT_PATH/firmware_version`
-if [ -z "${OEM_tmp}" ] ; then
-OEM_tmp=$CONFIG_OEM_DEFAULT
-fi
-OEM=${OEM_tmp%,*}
-OEM_DEFAULT_INDEX=${OEM_tmp#*,}
-if [ "$OEM_DEFAULT_INDEX" = "$OEM" ] ; then
+OEM=$CONFIG_OEM_DEFAULT
 OEM_DEFAULT_INDEX=""
-fi
+#OEM_tmp=`cat $CONFIG_ENVIRONMENT_PATH/firmware_version`
+#if [ -z "${OEM_tmp}" ] ; then
+#OEM_tmp=$CONFIG_OEM_DEFAULT
+#fi
+#OEM=${OEM_tmp%,*}
+#OEM_DEFAULT_INDEX=${OEM_tmp#*,}
+#if [ "$OEM_DEFAULT_INDEX" = "$OEM" ] ; then
+#OEM_DEFAULT_INDEX=""
+#fi
 export OEM_DEFAULT_INDEX
 export OEM
 ##########################################################################################
@@ -408,16 +410,16 @@
 ##########################################################################################
 ## retail
 ##########################################################################################
-if [ "$CONFIG_RETAIL" = "y" ] ; then
-testretail=`grep DMC ${CONFIG_ENVIRONMENT}`
-testretail=`echo ${testretail##DMC} | tr -d ' '`
-for i in `echo ${testretail} | tr ',' ' '` ; do
-case $i in
-RTL=y) export CONFIG_RETAIL="y"; break; ;;
-RTL=n) export CONFIG_RETAIL="n"; break; ;;
-esac;
-done
-fi
+#if [ "$CONFIG_RETAIL" = "y" ] ; then
+#testretail=`grep DMC ${CONFIG_ENVIRONMENT}`
+#testretail=`echo ${testretail##DMC} | tr -d ' '`
+#for i in `echo ${testretail} | tr ',' ' '` ; do
+#case $i in
+#RTL=y) export CONFIG_RETAIL="y"; break; ;;
+#RTL=n) export CONFIG_RETAIL="n"; break; ;;
+#esac;
+#done
+#fi
 ##########################################################################################
 ## Webguipass
 ##########################################################################################
