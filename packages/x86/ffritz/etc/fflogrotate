#!/bin/sh
#
LOGFILES=/var/tmp/ffritz.log

while true; do
    sleep 1200

    for f in $LOGFILES; do
	if [ -f $f -a `wc -c < $f` -gt 250000 ]; then
	    cp ${f} ${f}.1
	    echo > $f
	fi
    done
done
