#!/bin/sh
#
# Called by the mpd pipe driver for music output
#
. /var/media/ftp/ffritz/etc/ffritz-env.sh

# Volume level is communicated from mpd to usbplay via /var/tmp/volume
# (see mpd.conf)
#
$FFRITZ_HOME/bin/usbplay -V /var/tmp/volume >/dev/null
