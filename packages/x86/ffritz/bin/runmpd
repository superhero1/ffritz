#!/bin/sh
#
# Wrapper to call mpd with the proper libraries and config file
#
. /var/media/ftp/ffritz/etc/ffritz-env.sh

export HOME=$FFRITZ_HOME
$FFRITZ_HOME/bin/mpd $FFRITZ_HOME/etc/mpd.conf
exit $?
